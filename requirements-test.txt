# Testing Dependencies

# Core testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-timeout>=2.2.0
pytest-xdist>=3.5.0  # Parallel test execution

# Mocking
responses>=0.24.0  # Mock HTTP requests
# freezer>=1.2.0  # REMOVED: PyMI build fails on Windows
# Use pytest-mock + time.sleep mocking instead

# Coverage
coverage[toml]>=7.4.0

# Code quality (optional but recommended)
pylint>=3.0.0
flake8>=7.0.0
mypy>=1.8.0
black>=24.0.0
isort>=5.13.0

# Documentation testing
pytest-doctestplus>=1.0.0
